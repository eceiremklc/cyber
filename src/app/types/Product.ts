export type Product = {
  id: number | string;
  title: string;
  price: number;
  compareAtPrice: number;
  discountPercentage: number;
  description: string;
  image: string;
  category: string;
  quantity: number;
};
